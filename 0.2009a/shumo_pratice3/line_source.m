%% 无限长线源扩散模型,计算目标点浓度。
Q=0.0025;%污染源强度，单位时间污染物排放量。
v=3.2;%风速
x=300;%目标点与污染源距离。
sigma_z=0.14*x*power((1+0.0003*x),-0.5);
sigma_y=0.06.*x./((1+0.0015*x).^0.5);
H=0;%污染源高度。

%% 无限长线源扩散模型。
C=(2*Q)/(sqrt(2*pi)*v*sigma_z)*(exp(-power(H,2)/(2*power(sigma_z,2)))); %目标点污染浓度。

%% 

